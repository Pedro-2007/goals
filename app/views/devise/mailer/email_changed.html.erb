<p class="text-gray-800 text-lg font-medium mb-4">Olá <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
<p class="text-gray-700 bg-white p-4 rounded-lg shadow-sm border border-gray-200">
    Estamos entrando em contato para notificar que seu email está sendo alterado para
    <span class="text-blue-600 font-medium"><%= @resource.unconfirmed_email %></span>.
</p>
<% else %>
<p class="text-gray-700 bg-white p-4 rounded-lg shadow-sm border border-gray-200">
    Estamos entrando em contato para notificar que seu email foi alterado para
    <span class="text-blue-600 font-medium"><%= @resource.email %></span>.
</p>
<% end %>